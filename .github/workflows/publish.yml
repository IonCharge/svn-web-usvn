name: publish
on: [push]
jobs:
publish-svn-web-usvn-image:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v2
- name: Login to GitHub Container Registry
uses: docker/login-action@v1
with:
registry: ghcr.io
username: ${{ github.actor }}
password: ${{ secrets.GITHUB_TOKEN }}
- name: Build the svn-web-usvn Docker image
run: |
       docker build . --tag ghcr.io/ioncharge/svn-web-usvn:latest
       docker run ghcr.io/ioncharge/svn-web-usvn:latest
       docker push ghcr.io/ioncharge/svn-web-usvn:latest
